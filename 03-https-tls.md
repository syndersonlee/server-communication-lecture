# 03. HTTPS와 TLS

> HTTPS는 "HTTP + 보안". 그 보안을 책임지는 게 **TLS**입니다.
> 백엔드 개발자는 인증서 갱신, mTLS, SSL 핸드셰이크 오류를 종종 마주칩니다.

---

## 3.1 왜 HTTPS인가

HTTP는 평문(plain text) 통신입니다. 중간자(공유기, ISP, 카페 와이파이)가 들여다보면
로그인 비밀번호, 결제 정보가 그대로 노출됩니다.

HTTPS는 **TLS(Transport Layer Security)** 로 통신을 암호화하여 3가지를 보장합니다.

1. **기밀성(Confidentiality)**: 암호화 → 도청해도 못 읽음.
2. **무결성(Integrity)**: 변조되면 감지.
3. **인증(Authentication)**: 내가 접속한 서버가 진짜 그 서버임을 보증 (인증서).

> HTTPS = HTTP over TLS. 기본 포트는 **443**.

---

## 3.2 대칭키 vs 비대칭키 (암호화의 두 축)

### 대칭키(Symmetric)
- 암호화/복호화에 **같은 키** 사용. (예: AES)
- 빠르다. 하지만 **키를 어떻게 안전하게 공유?** 가 문제.

### 비대칭키(Asymmetric, 공개키 암호)
- **공개키(public)** 와 **개인키(private)** 쌍. (예: RSA, ECDSA)
- 공개키로 암호화 → 개인키로만 복호화. (반대도 가능: 서명)
- 느리지만 키 공유 문제를 해결.

> 💡 TLS는 **둘을 조합**합니다.
> 1) 처음엔 비대칭키로 "대칭키를 안전하게 교환"하고,
> 2) 이후 실제 데이터는 빠른 대칭키로 주고받음.
> → 보안과 속도를 모두 챙기는 영리한 설계.

---

## 3.3 인증서와 CA

서버의 공개키가 "진짜 그 서버 것"임을 어떻게 믿나? → **인증서(Certificate)** 와 **CA(인증기관)**.

- 서버는 **CA(Certificate Authority)** 에게 자기 공개키를 검증받아 **인증서**를 발급받음.
- 인증서에는 서버 도메인, 공개키, CA의 디지털 서명이 담김.
- 브라우저/OS에는 신뢰하는 CA 목록(Root CA)이 내장 → 그 CA가 서명한 인증서면 신뢰.

```
[신뢰 체인]
Root CA  →  중간 CA  →  서버 인증서(yanolja.com)
(브라우저가 신뢰)        (실제 사이트)
```

> 💡 Let's Encrypt 덕분에 무료 인증서 발급이 일반화. 보통 90일마다 자동 갱신.
> ⚠️ "인증서 만료"로 인한 장애는 실무에서 정말 흔합니다. 갱신 자동화 + 모니터링 필수.

---

## 3.4 TLS 핸드셰이크 (TLS 1.2 기준)

```
Client                                       Server
  │  ① ClientHello                              │
  │    (지원 TLS버전, 암호스위트 목록, 랜덤값)    │
  │ ─────────────────────────────────────────▶ │
  │                                             │
  │  ② ServerHello + 인증서 + ServerHelloDone   │
  │    (선택한 암호스위트, 서버 랜덤값, 공개키)   │
  │ ◀───────────────────────────────────────── │
  │                                             │
  │  ③ 인증서 검증 (CA 서명 확인)                │
  │     pre-master secret 생성 후 서버 공개키로  │
  │     암호화하여 전송                          │
  │ ─────────────────────────────────────────▶ │
  │                                             │
  │  양쪽이 동일한 '세션 대칭키' 도출             │
  │  ④ Finished (이후부터 대칭키로 암호화 통신)  │
  │ ◀─────────────────────────────────────────▶ │
```

- **TLS 1.2**: 위처럼 2-RTT 정도 소요.
- **TLS 1.3**: 핸드셰이크를 1-RTT로 단축, 보안 취약 알고리즘 제거. (현재 권장)

> 💡 앞 챕터의 HTTP/3(QUIC)는 이 TLS 핸드셰이크를 전송 연결 수립과 합쳐 더 빠르게 만듦.

---

## 3.5 mTLS (상호 TLS)

- 일반 TLS는 **클라이언트가 서버만** 검증.
- **mTLS(mutual TLS)**: 서버도 클라이언트 인증서를 검증. **양방향 인증**.
- 서비스 간 통신(MSA 내부, 제로 트러스트) 보안에 많이 사용. (예: Istio 같은 서비스 메시)

---

## 3.6 Spring / 운영 관점 팁

### 애플리케이션에서 직접 HTTPS 설정 (예: 내부 통신)

```yaml
# application.yml
server:
  port: 8443
  ssl:
    enabled: true
    key-store: classpath:keystore.p12
    key-store-password: ${KEYSTORE_PASSWORD}
    key-store-type: PKCS12
```

### 실무에서의 일반적인 구성

> 💡 보통 애플리케이션(톰캣)에 직접 TLS를 물리기보다,
> **앞단의 L7 로드밸런서(ALB, Nginx, API Gateway)에서 TLS 종료(termination)** 를 하고,
> 내부망은 HTTP 또는 mTLS로 통신하는 구성을 많이 씁니다.
> → 인증서 관리가 한 곳에 집중되고, 애플리케이션은 비즈니스에 집중.

### 자주 만나는 오류

- `SSLHandshakeException: PKIX path building failed`
  - 클라이언트(JVM)의 truststore에 상대 서버의 CA 인증서가 없을 때.
  - 사내 자체 서명 인증서를 쓰는 경우 truststore에 추가 필요.
- 인증서 도메인 불일치(`CN`/`SAN` mismatch).

---

## 핵심 요약

- HTTPS = HTTP over **TLS**. 기밀성·무결성·인증을 보장. 포트 443.
- TLS는 **비대칭키로 대칭키를 교환**한 뒤, 실제 데이터는 **대칭키**로 암호화 → 보안 + 속도.
- **인증서 + CA 신뢰 체인**으로 서버 신원을 검증.
- **mTLS**는 서버·클라이언트 양방향 인증, MSA 내부 보안에 사용.
- 실무에선 **LB/게이트웨이에서 TLS 종료**가 일반적이며, **인증서 만료 모니터링**이 중요.

## 면접/실무 체크리스트

- [ ] TLS가 대칭키와 비대칭키를 둘 다 쓰는 이유는?
- [ ] 인증서가 "신뢰"되는 원리(CA 체인)는?
- [ ] TLS 1.3이 1.2 대비 개선한 점은? (1-RTT, 취약 알고리즘 제거)
- [ ] mTLS는 일반 TLS와 무엇이 다른가?

---

이전 → [02. HTTP 완전 분석](./02-http.md) | 다음 → [04. 실시간 통신 ① Polling](./04-polling.md)
