# 10. 기술 선택 가이드 (종합 정리)

> 지금까지 배운 모든 통신 기법을 한자리에 모아 비교하고,
> "그래서 실무에서 뭘 골라야 하나"를 의사결정 트리와 시나리오로 정리합니다.

---

## 10.1 전체 기술 비교표

| 기술 | 계층/기반 | 방향 | 동기/비동기 | 실시간성 | 주 용도 |
|------|-----------|------|:-----------:|:--------:|---------|
| **HTTP/REST** | TCP(HTTP1.1/2), QUIC(3) | 요청-응답 | 동기 | 낮음 | 범용 API, CRUD |
| **gRPC** | HTTP/2 | 단방향+스트리밍 | 동기 | 높음 | 서버 간 고성능 통신(MSA) |
| **GraphQL** | HTTP | 요청-응답 | 동기 | 낮음 | 유연한 데이터 조회(BFF) |
| **Short Polling** | HTTP | 단방향 반복 | 동기 | 낮음 | 가끔 갱신, 단순 |
| **Long Polling** | HTTP | 단방향(보류) | 동기 | 중간 | 레거시 실시간 |
| **SSE** | HTTP | 서버→클라 단방향 | 동기(스트림) | 높음 | 알림/피드/시세/LLM 스트림 |
| **WebSocket** | TCP(ws/wss) | **양방향** | 동기(스트림) | 매우 높음 | 채팅/게임/협업 |
| **MQ(RabbitMQ)** | AMQP | pub/sub·큐 | 비동기 | - | 작업 분배, 디커플링 |
| **Kafka** | TCP | pub/sub | 비동기 | - | 이벤트 스트리밍, EDA |
| **MQTT** | TCP | pub/sub | 비동기 | 높음 | IoT/모바일 |

---

## 10.2 의사결정 트리

```
Q1. 통신 시점이 즉시 응답이 필요한가, 아니면 던져놓고 잊어도 되는가?

  ├─ [던져놓고 잊는다 / 디커플링·대용량 이벤트] → 비동기 메시징
  │     ├─ 대용량 이벤트 스트림, 재처리 필요  → Kafka
  │     ├─ 복잡한 라우팅, 작업 큐            → RabbitMQ
  │     └─ IoT/저전력 디바이스               → MQTT
  │
  └─ [즉시 응답 필요 / 동기] → Q2로

Q2. 실시간으로 데이터를 계속 받아야 하는가?

  ├─ [아니오, 단발성 요청-응답] → Q3로
  │
  └─ [예, 실시간] → Q4로

Q3. (단발성) 어떤 API 스타일?
  ├─ 서버 간 고성능/저지연, polyglot     → gRPC
  ├─ 클라이언트마다 다른 데이터 조합 필요 → GraphQL
  └─ 그 외 범용/표준/캐싱 중요            → REST(HTTP)

Q4. (실시간) 양방향이 필요한가?
  ├─ [양방향 필요(채팅/게임/협업)]        → WebSocket
  ├─ [서버→클라 단방향이면 충분(알림/피드)]→ SSE
  └─ [위 기술 사용 불가한 제약 환경]       → Long Polling
```

---

## 10.3 한 줄 선택 가이드

- **그냥 API가 필요하다** → REST. 가장 무난하고 생태계가 크다.
- **서버끼리 빠르게 많이 호출한다(MSA 내부)** → gRPC.
- **프론트가 데이터를 자유롭게 조합해야 한다** → GraphQL.
- **서버가 클라에게 계속 보내기만 하면 된다(알림, 시세, LLM)** → SSE.
- **서로 실시간으로 주고받아야 한다(채팅, 게임)** → WebSocket.
- **즉시 응답이 필요 없고, 시스템을 느슨하게 연결하고 싶다** → Kafka / RabbitMQ.
- **수많은 저전력 디바이스(IoT)** → MQTT.

---

## 10.4 실전 시나리오 (야놀자 스타일 예시)

### 시나리오 1 — 숙소 예약 API
- 앱/웹에서 예약 생성·조회. → **REST** (표준 CRUD, 캐싱, 넓은 호환성).
- 결제 게이트웨이 호출처럼 멱등성이 중요한 곳은 **멱등키** 적용 (02 챕터).

### 시나리오 2 — 예약 완료 후 후속 처리
- 예약 완료 → 결제 정산, 알림 발송, 추천 데이터 갱신, 데이터 분석.
- → **Kafka**로 `reservation.completed` 이벤트 발행. 각 서비스가 독립 소비 (9장 EDA).
- 응답 지연 없이 즉시 사용자에게 "예약 완료" 반환.

### 시나리오 3 — 실시간 예약 현황/알림
- "방금 막 누가 이 방을 예약했어요" 같은 실시간 알림.
- → **SSE** (서버→클라 단방향이면 충분). 다중 서버는 Redis Pub/Sub로 fan-out (5장).

### 시나리오 4 — 고객센터 실시간 채팅
- 상담원 ↔ 고객 양방향 대화.
- → **WebSocket + STOMP**. 스케일아웃은 외부 브로커(RabbitMQ relay) (6장).

### 시나리오 5 — 내부 마이크로서비스 간 통신
- 예약 서비스 → 재고 서비스, 가격 서비스 호출 (저지연 다수 호출).
- → **gRPC** (7장). 외부 공개는 REST, 내부는 gRPC의 전형적 구성.

### 시나리오 6 — AI 컨시어지(LLM) 답변 스트리밍
- 토큰을 생성되는 대로 화면에 흘려보냄.
- → **SSE** (LLM 스트리밍의 사실상 표준).

---

## 10.5 성능·확장 공통 원칙

1. **연결은 비싸다** → 재사용하라 (Keep-Alive, 커넥션 풀, HTTP/2 멀티플렉싱).
2. **단방향이면 양방향을 쓰지 마라** → SSE로 될 일에 WebSocket을 쓰면 복잡도만 증가.
3. **즉시 응답이 불필요하면 비동기로** → 사용자 응답 시간에서 무거운 작업을 빼라.
4. **재시도엔 멱등성을** → 중복 결제/중복 처리 방지 (멱등키, 멱등 컨슈머).
5. **실시간 다중 서버는 fan-out 설계** → Redis Pub/Sub, 외부 브로커.
6. **장애를 전제로 설계** → 타임아웃, 서킷브레이커, DLQ, 백프레셔.

---

## 10.6 전체 강의 한 장 요약

```
[동기 · 요청-응답]
  REST     : 범용. 일단 이걸로 시작.
  gRPC     : 서버 간 고성능. 바이너리+HTTP/2.
  GraphQL  : 필요한 데이터만. 유연한 조회.

[동기 · 실시간]
  Polling      : 단순하지만 비효율. (레거시)
  Long Polling : 보류 응답. (레거시 실시간)
  SSE          : 서버→클라 단방향 스트림. 알림/LLM.
  WebSocket    : 진짜 양방향. 채팅/게임.

[비동기 · 메시징]
  RabbitMQ : 라우팅/작업 큐. 소비 후 삭제.
  Kafka    : 대용량 이벤트 스트림. 보존/재처리. EDA 중추.
  MQTT     : IoT/모바일 초경량.

[보안]
  HTTPS/TLS: 모든 통신의 기본. 대칭키+비대칭키 조합. 인증서 관리가 운영의 핵심.
```

---

## 마무리

이 강의에서 우리는 가장 낮은 **소켓·TCP/UDP**부터, **HTTP의 진화(1.1→2→3)**,
실시간 3종(**Polling → SSE → WebSocket**), 서버 간 통신(**gRPC**),
유연한 조회(**GraphQL**), 그리고 비동기(**Kafka/RabbitMQ/MQTT**)까지 훑었습니다.

핵심은 **"정답인 기술은 없고, 상황에 맞는 기술이 있을 뿐"** 이라는 것입니다.
각 기술이 **어떤 문제를 풀기 위해 등장했는지**(= 이전 기술의 어떤 한계를 극복했는지)를
이해하면, 새로운 기술이 나와도 그 자리를 금방 파악할 수 있습니다.

수고하셨습니다! 🎓

---

이전 → [09. 비동기 메시징](./09-messaging.md) | 처음 → [00. 오리엔테이션](./README.md)
